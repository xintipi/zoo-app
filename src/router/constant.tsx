import { lazy } from 'react';

/**
 * @description: default layout
 */
export const DefaultLayout = lazy(() => import('@/layouts/Default/Default'));

/**
 * @description: default base
 */
export const BaseLayout = lazy(() => import('@/layouts/Base/Base'));
